import React from "react";
import { Chart } from "react-google-charts";
import { Typography } from "@mui/material";

export const VisualView = () => {
  return <Typography>Hello visual view</Typography>;
};
