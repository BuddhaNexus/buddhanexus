export interface DatabaseText {
  label: string;
  id: string;
  name: string;
  fileName: string;
  textName: string;
  category: string;
}

export interface DatabaseCategory {
  id: string;
  categoryName: string;
  name: string;
}
